node_modules/

# SPEC §2: every secret lives in .env; only the placeholder file is tracked.
.env*
!.env.example

# Build outputs
dist/
build/

# Misc
.DS_Store
npm-debug.log*

# Ignore ONLY database dumps/backups (not migrations)
# If you save backups in a specific folder, list that here instead
backup_*.sql
*.bak

# Database dumps and poller output — never committed
backups/
logs/
